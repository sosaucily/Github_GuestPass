 Github_GuestPass
====================

Github_GuestPass give repository owners the ability to grant temporary read-only access to a private GitHub repository.
---------------------

Current plan:

* Authentication is granted via OAuth, using GitHub API v3
* System is written in ruby, likely with Sinatra backend
